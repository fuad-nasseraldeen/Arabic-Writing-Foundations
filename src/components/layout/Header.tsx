"use client";
import Link from "next/link"; import { Menu, Search, X } from "lucide-react"; import { useState } from "react"; import { navigation } from "@/data/navigation";
export function Brand() { return <Link href="/" className="brand" aria-label="קדם־כתיבה בערבית, דף הבית"><span className="leaf" aria-hidden="true">❧</span><span>קדם־כתיבה<br /><b>בערבית</b></span></Link>; }
export function Header() { const [open,setOpen]=useState(false); return <header className="header"><div className="nav-wrap"><Brand /><nav className="desktop-nav" aria-label="ניווט ראשי">{navigation.map((n)=><Link key={n.href} href={n.href}>{n.label}</Link>)}</nav><div className="nav-actions"><button className="icon-button search" aria-label="חיפוש"><Search size={20}/></button><button className="icon-button menu" aria-label={open?"סגירת תפריט":"פתיחת תפריט"} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="mobile-nav" aria-label="ניווט בנייד">{navigation.map((n)=><Link key={n.href} href={n.href} onClick={()=>setOpen(false)}>{n.label}</Link>)}</nav>}</header>; }
