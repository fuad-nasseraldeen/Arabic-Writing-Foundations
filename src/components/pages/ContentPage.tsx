import Link from "next/link"; import { ArrowLeft } from "lucide-react";
export function ContentPage({eyebrow,title,description,items}:{eyebrow:string;title:string;description:string;items:string[]}){return <div className="page-shell container"><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p className="lead">{description}</p><div className="content-list">{items.map((item,i)=><article key={item}><span>0{i+1}</span><h2>{item}</h2><p>אזור תוכן מוכן להרחבה ולעריכה בהתאם לחומר המקצועי שייאסף לפרויקט.</p><Link href="/project">לפרטים <ArrowLeft size={15}/></Link></article>)}</div></div>}
