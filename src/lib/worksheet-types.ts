export type FilterOption = { id: string; group_id: string; key: string; label_he: string; label_ar: string; sort_order: number; is_visible: boolean };
export type FilterGroup = { id: string; key: string; label_he: string; label_ar: string; selection_mode: "single" | "multi"; sort_order: number; is_visible: boolean; options: FilterOption[] };
export type LetterGroup = { id: string; title_he: string | null; title_ar: string | null };
export type Worksheet = { id: string; title_he: string; title_ar: string | null; description_he: string | null; description_ar: string | null; therapeutic_goal_he: string | null; therapeutic_goal_ar: string | null; letter_group_id: string | null; thumbnail_url: string | null; file_url: string | null; file_type: "image" | "pdf" | null; original_file_name: string | null; is_visible: boolean; sort_order: number; created_at: string; updated_at: string };
export type WorksheetLibraryData = { worksheets: Worksheet[]; filterGroups: FilterGroup[]; worksheetOptionIds: Record<string, string[]>; letterGroups: LetterGroup[]; columns: number };
export const localized = (row: Record<string, unknown>, field: string, locale: "he" | "ar") => String(row[`${field}_${locale}`] || row[`${field}_he`] || "");
