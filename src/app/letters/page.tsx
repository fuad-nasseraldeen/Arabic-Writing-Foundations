import Link from "next/link"; import { Search, ArrowLeft } from "lucide-react"; import { letterGroups, letters } from "@/data/letters";
export default function LettersPage(){return <div className="page-shell container"><span className="eyebrow">מפת אותיות – טיוטת תוכן</span><h1>אותיות בקבוצות</h1><p className="lead">ארגון ראשוני של אותיות לפי מאפיינים חזותיים, מבניים ומוטוריים. החלוקה נמצאת בתהליך פיתוח ואינה מהווה סיווג מדעי מאומת.</p><div className="letters-tools"><label className="search-field"><Search size={19}/><input placeholder="חיפוש אות או קבוצה" aria-label="חיפוש אות או קבוצה"/></label><div className="chips"><button>הכול</button><button>חזותי</button><button>מוטורי</button><button>טיוטה</button></div></div><div className="group-grid">{letterGroups.map(group=><article className="group-card" key={group.id}><span className="eyebrow">טיוטת קבוצה</span><h2>{group.title}</h2><p>{group.description}</p><div className="letter-row arabic">{group.letters.map(l=><span key={l}>{l}</span>)}</div>{group.letters.includes("ب")&&<Link href="/letters/baa" className="text-link">לניתוח האות ب <ArrowLeft size={16}/></Link>}</article>)}</div><section className="featured-letter"><div className="arabic">ب</div><div><span className="eyebrow">כניסה לדוגמה</span><h2>האות الباء</h2><p>דף אב־טיפוס המדגים את מבנה הניתוח המקצועי שייושם בהמשך עבור אותיות נוספות.</p><Link className="button" href="/letters/baa">לפתיחת הדף <ArrowLeft size={17}/></Link></div></section></div> }
